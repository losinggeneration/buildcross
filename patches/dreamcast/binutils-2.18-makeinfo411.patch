
--- /dev/null	Thu Jan 01 00:00:00 1970 +0000
+++ b/SOURCES/binutils-2.18-makeinfo411.patch	Fri Dec 07 17:03:38 2007 -0500
@@ -0,0 +1,25 @@
+diff -ruN baseline-binutils-2.18/configure binutils-2.18/configure
+--- baseline-binutils-2.18/configure	2007-08-06 16:29:40.000000000 -0400
++++ binutils-2.18/configure	2007-11-12 23:48:23.000000000 -0500
+@@ -6128,7 +6128,7 @@
+     # For an installed makeinfo, we require it to be from texinfo 4.4 or
+     # higher, else we use the "missing" dummy.
+     if ${MAKEINFO} --version \
+-       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|[5-9])' >/dev/null 2>&1; then
++       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|4\.[1-9][0-9]|[5-9])' >/dev/null 2>&1; then
+       :
+     else
+       MAKEINFO="$MISSING makeinfo"
+diff -ruN baseline-binutils-2.18/configure.ac binutils-2.18/configure.ac
+--- baseline-binutils-2.18/configure.ac	2007-08-28 16:24:26.000000000 -0400
++++ binutils-2.18/configure.ac	2007-11-12 23:50:33.000000000 -0500
+@@ -2403,7 +2403,7 @@
+     # For an installed makeinfo, we require it to be from texinfo 4.4 or
+     # higher, else we use the "missing" dummy.
+     if ${MAKEINFO} --version \
+-       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|[5-9])' >/dev/null 2>&1; then
++       | egrep 'texinfo[^0-9]*([1-3][0-9]|4\.[4-9]|4\.[1-9][0-9]|[5-9])' >/dev/null 2>&1; then
+       :
+     else
+       MAKEINFO="$MISSING makeinfo"
+diff -ruN baseline-binutils-2.18/gas/configure.tgt binutils-2.18/gas/configure.tgt




