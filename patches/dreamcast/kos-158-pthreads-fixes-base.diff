Index: kos-1.3.0/kos/utils/vqenc/vqenc.c
===================================================================
--- kos-1.3.0/kos/utils/vqenc/vqenc.c	(revision 158)
+++ kos-1.3.0/kos/utils/vqenc/vqenc.c	(working copy)
@@ -23,7 +23,7 @@
 #include "vq_types.h"
 
 /* For outputting KMG files */
-#include "../../addons/include/kmg/kmg.h"
+#include "../../../kos-ports/include/kmg/kmg.h"
 
 static int use_mipmap = 0;
 static int use_twiddle = 0;
Index: kos-1.3.0/kos/Makefile
===================================================================
--- kos-1.3.0/kos/Makefile	(revision 158)
+++ kos-1.3.0/kos/Makefile	(working copy)
@@ -10,7 +10,7 @@
 ifdef KOS_CCPLUS
 	DIRS += libk++
 endif
-DIRS += kernel addons # examples
+DIRS += kernel addons ../kos-ports/lib* ../kos-ports/lua ../kos-ports/lwip ../kos-ports/SDL ../kos-ports/zlib examples
 
 # Detect a non-working or missing environ.sh file.
 ifndef KOS_BASE
Index: kos-1.3.0/kos/examples/dreamcast/lua/Makefile
===================================================================
--- kos-1.3.0/kos/examples/dreamcast/lua/Makefile	(revision 158)
+++ kos-1.3.0/kos/examples/dreamcast/lua/Makefile	(working copy)
@@ -6,7 +6,7 @@
 # $Id: Makefile,v 1.1 2002/06/13 09:51:16 bardtx Exp $
 
 all:
-	$(KOS_MAKE) -C basic
+#	$(KOS_MAKE) -C basic
 
 clean:
 	$(KOS_MAKE) -C basic clean
Index: kos-1.3.0/kos/examples/dreamcast/sound/Makefile
===================================================================
--- kos-1.3.0/kos/examples/dreamcast/sound/Makefile	(revision 158)
+++ kos-1.3.0/kos/examples/dreamcast/sound/Makefile	(working copy)
@@ -6,10 +6,10 @@
 # $Id: Makefile,v 1.2.2.1 2003/06/20 06:31:15 bardtx Exp $
 
 all:
-	$(KOS_MAKE) -C ghettoplay-vorbis
-	$(KOS_MAKE) -C hello-ogg
-	$(KOS_MAKE) -C hello-mp3
-	$(KOS_MAKE) -C cdda
+#	$(KOS_MAKE) -C ghettoplay-vorbis
+#	$(KOS_MAKE) -C hello-ogg
+#	$(KOS_MAKE) -C hello-mp3
+#	$(KOS_MAKE) -C cdda
 
 clean:
 	$(KOS_MAKE) -C ghettoplay-vorbis clean
Index: kos-1.3.0/kos/examples/dreamcast/parallax/Makefile
===================================================================
--- kos-1.3.0/kos/examples/dreamcast/parallax/Makefile	(revision 158)
+++ kos-1.3.0/kos/examples/dreamcast/parallax/Makefile	(working copy)
@@ -5,7 +5,7 @@
 #   
 # $Id: Makefile,v 1.2 2002/09/10 04:40:10 bardtx Exp $
 
-TARGETS = font bubbles raster_melt sinus delay_cube rotocube
+TARGETS = font bubbles raster_melt sinus delay_cube rotocube serpent_dma
 
 all:
 	for i in $(TARGETS); do $(KOS_MAKE) -C $$i || exit -1; done
Index: kos-1.3.0/kos/examples/dreamcast/conio/Makefile
===================================================================
--- kos-1.3.0/kos/examples/dreamcast/conio/Makefile	(revision 158)
+++ kos-1.3.0/kos/examples/dreamcast/conio/Makefile	(working copy)
@@ -8,8 +8,8 @@
 all:
 	$(KOS_MAKE) -C basic
 	$(KOS_MAKE) -C wump
-	$(KOS_MAKE) -C adventure
-	$(KOS_MAKE) -C kosh
+#	$(KOS_MAKE) -C adventure
+#	$(KOS_MAKE) -C kosh
 
 clean:
 	$(KOS_MAKE) -C basic clean
Index: kos-1.3.0/kos/examples/dreamcast/kgl/demos/tunnel/menu.cpp
===================================================================
--- kos-1.3.0/kos/examples/dreamcast/kgl/demos/tunnel/menu.cpp	(revision 158)
+++ kos-1.3.0/kos/examples/dreamcast/kgl/demos/tunnel/menu.cpp	(working copy)
@@ -56,7 +56,7 @@
 	uf2i.f = min; madd->min = uf2i.i;
 	uf2i.f = max; madd->max = uf2i.i;
 	uf2i.f = amt; madd->amt = uf2i.i;
-	(float *)madd->pvalue = (float *)pval;
+	*((float **)&madd->pvalue) = (float *)pval;
 	madd->pformat = (char*) malloc(strlen(pformat)+1); 
 	memcpy(madd->pformat, pformat, strlen(pformat)+1);
 	printf("[f**]menu->add = %d\n", strlen(pformat)+1);
